{
  "identifier": "default",
  "description": "Default capabilities for LocalTeam",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:allow-open"
  ]
}
